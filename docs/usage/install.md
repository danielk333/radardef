# Install
---
radardef is available as a pip package:
```bash
   pip install radardef
```

or it can be installed from the source

```bash
   git clone --branch develop git@github.com:danielk333/radardef.git
   cd radardef
   pip install .
```

Read the CLI description

```bash
   radardef -h
```
